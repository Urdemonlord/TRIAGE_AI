[languages.python]
version = "3.11"

[build]
nixpkgs.url = "github:NixOS/nixpkgs/nixpkgs-unstable"

[phases.install]
cmds = ["cd ai-service && pip install -r requirements.txt"]

[phases.build]
cmds = []

[[processes]]
name = "web"
cmd = "cd ai-service && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
